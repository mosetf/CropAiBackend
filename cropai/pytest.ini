# Pytest configuration is intentionally centralized at the repository root.
# Keeping a second active pytest.ini in this subdirectory can cause
# working-directory-dependent test discovery and import behavior.
# Use the root-level pytest.ini as the single source of truth.
